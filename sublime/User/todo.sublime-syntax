%YAML 1.2
---
# http://www.sublimetext.com/docs/3/syntax.html
name: todo
file_extensions:
  - todo
scope: text.plain
contexts:
  main:
    - match: '^\s*-+$'
      scope: comment
    - match: '^\s*[-*o>•▶▬]'
      scope: comment
    - match: '^=+$'
      scope: comment
    - match: '^$'
      push: old
    # - match: '\('
    #   push: paren

  # paren:
  #   - meta_scope: comment
  #   - match: '\('
  #     push: paren
  #   - match: '\)'
  #     pop: true

  old:
    - meta_scope: comment
